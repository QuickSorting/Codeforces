#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
#define all(x) (x).begin(), (x).end()

const int N = 1e5 + 3;

int n, m1, m2;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> n >> m1 >> m2;
    for(int i = 0; i < m1; ++i)
}